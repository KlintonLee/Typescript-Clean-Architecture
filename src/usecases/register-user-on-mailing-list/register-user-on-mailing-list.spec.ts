import { IUserData } from './dtos/user-data'

describe('register-user-on-mailing-list.spec.ts', () => {
  it('should add user with complete data on mailing list', async () => {
    const users: Array<IUserData> = []
    console.log(users)
    // const repo: UsersRepository = new InMemoryUsersRepository(users)
    // const usecase: RegisterUserOnMailingList = new RegisterUserOnMailingList(repo)

    // const name = 'John Doe'
    // const email = 'john.doe@email.com'
    // const response = await usecase.registerUserOnMailingList({ name, email })
    // const user = await repo.findUserByEmail('john.doe@email.com')
    // expect(user.name).toBe('John Doe')
  })
})
